# さんすうクエスト v2 変更点

- ドット絵を廃止し、背景・勇者・敵キャラクターをイラスト調に全面刷新。
- タブレット横向きを主眼に、HUD・問題パネル・回答ボタンを再設計。
- 3項計算では問題式を自動で縮小し、表示枠からはみ出しにくいよう改善。
- ステージ移行時にエリア移動画面を追加し、約1.5秒の休憩を兼ねる演出へ変更。
- 進行ゲージ、残り時間、現在ステージの視認性を改善。
- BGMは今後ステージ別に差し替えられる前提の構造を維持。
- 問題生成ルール（繰り上がり・繰り下がりなし）は従来仕様を維持。

## v3 改修（2026-08-22）
- 数式表示を実測幅ベースの自動フィット方式へ変更。3項式を含む長い式でも問題パネルからはみ出さないようにした。
- 架空のワールドマップ `assets/world_map.png` を追加。
- ステージ遷移を「BGM停止 → マップ移動 → 次ステージ背景のみ表示 → 戦闘開始・BGM再生」の4段階へ変更。
- ステージデータに `bgm` を追加し、将来的なステージ別BGM差し替えに対応。
- 勇者・敵キャラクターに白～水色の発光縁取りを追加。発光強度が緩やかに揺らぐアニメーションを実装。

## v4 BGM割り当て
- ステージ1「はじまりの もり」: `Cybern.mp3`
- ステージ2「ふしぎな どうくつ」: `Cold Amber.mp3`
- ステージ3「まほうの とう」: `Crate Lockup Tango.mp3`
- ステージ4「まおうの しろ」: `Quantized Panic.mp3`
- ステージ5「まおうの へや」: `Geology.mp3`
- ステージ遷移開始時にBGMを停止し、次ステージ開始時に対応曲を先頭から再生する仕様に変更。
- 指定MP3が存在しない環境では既存 `bgm.mp3` に自動フォールバックする安全処理を追加。

## v5: ライフ制・問題GUI改修
- ライフを3に設定。
- 誤答・時間切れでライフを1消費。
- ライフが残っている間は進行度を維持して同じステージを継続。
- 3回目のミスで、その時点のステージ開始位置へ戻る。
  - はじまりの もり: 0/20
  - ふしぎな どうくつ: 5/20
  - まほうの とう: 10/20
  - まおうの しろ: 15/20
  - まおうの へや: 19/20
- 新ステージ到達時はライフを3に回復。
- HUDの「れんしょう」を「すすみ」に変更。
- 問題GUIを暗色戦闘パネル＋金縁フレームへ刷新。
- 解答3択を装飾フレーム付きの大型ボタンへ刷新。
- 数式表示を、表示領域の実寸幅・高さを計測して自動フィットする方式へ変更。

## v6
- 数式自動フィットを「比率縮小」から二分探索方式へ変更。毎回、表示枠に収まる最大フォントサイズを再計算するため、一部問題だけ極端に小さくなる現象を修正。
- 3項式を専用判定し、最大文字サイズ・最小文字サイズ・字間を個別設定。3項式の枠外表示を防止。
- ワールドマップを `world_map_v2.png` に刷新。
- 最終ステージ「まおうの へや」を「まおうの しろ」の内部に配置。ステージ4→5は世界を移動せず、城内へ進入する短い移動として表示。
- 最終ステージの背景プレビュー参照を `bg_boss.png` に修正。

## v7 改修
- 数式サイズが1問ごとに大きく／小さく揺れる問題を修正。表示中の実問題を直接測る方式をやめ、2項式・3項式それぞれの最長基準式を非表示プローブで計測し、形式ごとに一定の最大文字サイズを適用する方式へ変更。
- 数式フィット中は一時的に非表示とし、前問サイズが一瞬見えるフリッカーも防止。
- 3項式は「99 + 99 + 99 = ?」を基準にフィットするため、全ての3項式が問題枠に収まる設計へ変更。
- ワールドマップをリアル寄りの幻想世界マップへ再変更。地理関係は「はじまりの森→ふしぎな洞窟→まほうの塔→まおうの城」を維持し、魔王の部屋は城内部の拡大表示として扱う。
- マップ移動に光のスイープ、マーカー発光、ゆるいズーム、到着パルスを追加。
- ステージ遷移は、マップ移動約3.7秒＋次ステージ背景表示約1.25秒で、合計約5秒の休憩シークエンスへ調整。

## v8
- 問題ごとの自動フォントサイズ計測を完全に廃止。
- 数式を画面レイアウトごとの固定フォントサイズへ統一し、`11+22+33=66` 相当の幅が収まる設計に変更。
- 数式表示から演算子前後の余分な空白を除去し、長い3項式でも安定して中央表示。
- タイトル／ステージ名／数値／本文でフォント体系を分離し、ファンタジーRPG寄りのタイポグラフィへ刷新。
- タイトル画面を全面再設計。ルール説明を4枚の独立カードに分離し、「ライフ3」「20もん」が連結して見える問題を解消。
- 初回ステージも、ワールドマップ移動 → ステージ背景・名称表示 → BGM開始 → 第1問、の順で開始するよう統一。
